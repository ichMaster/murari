"""murari — the style engine: execute a style (a sequence of role moves) over a session,
with deterministic randomness, per-move budgets, target selection, and the DOCUMENT
ownership guard.

Filled in MUR-011.
"""
