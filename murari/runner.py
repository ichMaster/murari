"""murari — the AgentRunner seam: the verified `claude -p --append-system-prompt`
invocation with per-role tool narrowing, behind a mockable interface.

Filled in MUR-009 (ClaudeCliRunner + MockAgentRunner + the kickoff-prompt builder).
"""
