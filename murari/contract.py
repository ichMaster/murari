"""murari — the agent output contract (v2) and fence/preamble-tolerant JSON extraction.

Filled in MUR-007: the v2 schema (role, target_idea, mutation_type, hypotheses with
id/parents, born_from, next_role, nullable document_delta, dry_run) + `extract_contract`.
"""
